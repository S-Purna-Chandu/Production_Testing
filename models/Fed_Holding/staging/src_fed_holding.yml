version: 2

# Define the sources for your project
sources:
  - name: fed_staging
    database: '{{ env_var("DBT_FED_SRC_DB", target.database) }}'
    schema: '{{ env_var("DBT_FED_SRC_SCH", target.schema) }}'
    tables:
      - name: '{{ env_var("DBT_FED_SRC_EXT_STG", "fed_data") }}'
        description: FED holdings data in CSV format